
<a class="btn btn-info" href="{{ "{{ path('" ~ route_name_prefix ~ "') }}" }}">
    <span class="glyphicon glyphicon-arrow-left"></span>&nbsp;{{ "{{ 'Back to the list' | trans }}" }}
</a>
{% if ('edit' in actions) and (not hide_edit) %}
<a class="btn btn-warning" href="{{ "{{ path('" ~ route_name_prefix ~ "_edit', { 'id': entity.id }) }}" }}">
    <span class="glyphicon glyphicon-pencil"></span>&nbsp; {{' {{ "Edit" | trans }}' }}
</a>
{% endif %}
{% if ('delete' in actions) and (not hide_delete) %}
    {{ '{{ form(delete_form) }}' }}
{% endif %}